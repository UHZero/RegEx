const texto = 'Comida boa e comida colorida, comida boa e comida com amor, comida é ACOMIDA.'

const regex = /comida/gi
console.log(texto.match(regex))
console.log(texto.match(/a b/)) //retornsa input e index

